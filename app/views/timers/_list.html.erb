<% @collection.group_by(&:by_day).sort.reverse.each do |day, timers| %>
  <h4>
    <%= l day, :format => :long %>,
    <span class="clock" data-clock="<%= timers.map(&:amount).sum %>"></span>
  </h4>

  <ul class="timers">
    <%= render timers %>
  </ul>
<% end %>
